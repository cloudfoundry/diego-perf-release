---
name: orchestrator
templates:
  orchestrator_ctl.erb: bin/orchestrator_ctl

packages:
- orchestrator
- pid_utils

properties:
  orchestrator.listen_addr:
    description: "address at which to serve API requests"
    default: "127.0.0.1:2702"
  orchestrator.pusher_instances:
    description: Number of pusher instances
  orchestrator.log_level:
    description: Lager log level
    default: debug
